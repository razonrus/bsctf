@model SearchEngine.User[]

@{
    ViewBag.Title = "Search Page";
}

<div class="jumbotron">
    <h1>Search user</h1>
    @using (Html.BeginForm())
    {
  <input type="text"/>
        <p><button href="http://asp.net" class="btn btn-primary btn-lg">Search &raquo;</button>
        </p>
    }

</div>
@if (Model != null)
{
    <div class="row">
        Search results:
        <ul>
            @foreach (var user in Model)
            {
                <li>
                    @user.FirstName
                    &nbsp;
                    @user.LastName
                </li>
            }
        </ul>
    </div>
}